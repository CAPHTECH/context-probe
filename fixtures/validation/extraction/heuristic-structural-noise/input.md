# 構造ノイズ

この資料では `MCCS`、`score.compute`、`ReviewQueue` を用語として扱う。

この資料では `--docs-root`、`src/core/scoring.ts`、`FRG-doc-20-p-44`、`result.report`、`result.gate.failures`、`worst_acceptable`、`response_measure` は構造参照であり、用語として扱わない。
