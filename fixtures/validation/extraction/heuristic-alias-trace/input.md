# Heuristic alias trace

`Measurement Quality` (`measurementQuality`) is the run-level evidence-quality summary.

`Runtime Summary` (`runtime`) is the stage-based runtime metadata for one command run.
