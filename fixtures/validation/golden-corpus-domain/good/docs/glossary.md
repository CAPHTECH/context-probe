# Domain glossary good

`Invoice Contract` (`InvoiceContract`) is the public billing contract.

`Payment Summary` (`PaymentSummary`) is the billing read model used after invoice confirmation.

`Shipment Ticket` (`ShipmentTicket`) is the public fulfillment contract.

`Shipment Summary` (`ShipmentSummary`) is the fulfillment read model for shipment review.
